# -----------------------------
# Database (backend only)
# -----------------------------
# Local dev DB (Prisma/SQLite). Override in production with Postgres/MySQL/etc.
DATABASE_URL="file:./dev.db"

# -----------------------------
# Frontend flags (exposed to browser)
# -----------------------------
# Enable verbose logging in the browser console
VITE_DEBUG_LOGS=true

# -----------------------------
# Server
# -----------------------------
PORT=8080
NODE_ENV=development

# -----------------------------
# Project paths
# -----------------------------
# Absolute path to your repo root
PROJECT_ROOT=/absolute/path/to/expert-snapshot-legal

# Frontend source (for Vite dev server in development mode)
FRONTEND_SOURCE_PATH=/absolute/path/to/expert-snapshot-legal/frontend

# Frontend build output
FRONTEND_BUILD_PATH=/absolute/path/to/expert-snapshot-legal/frontend/build/frontend

# Storage for templates/manifests
STORAGE_PATH=/absolute/path/to/expert-snapshot-legal/frontend/storage

# -----------------------------
# Candidate extraction
# -----------------------------
# TTL for cached candidates (default 1h = 3600000 ms)
CANDIDATE_TTL_MS=3600000

# -----------------------------
# Azure Form Recognizer (secrets — do NOT commit real values)
# -----------------------------
AZURE_FORM_RECOGNIZER_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_FORM_RECOGNIZER_KEY=<your-secret-key>

